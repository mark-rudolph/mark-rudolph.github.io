# Include your project-specific ignores in this file
# Read about how to use .gitignore: 
# https://help.github.com/articles/ignoring-files
# in the discussion below root is the clone root, i.e
# the directory which contains .gitignore (this file)

# NOTE: ./node_modules can be built from package.json
# build node_modules locally from package.json by:
# root> npm install 
node_modules
docs
logs

# internal notes - last change
# dome resources, notes, printer-versions, hide from transpile, etc.
~*
*.txt
*.pr   
*-pr   
*-hide

# NOTE: ./dist can be re-built by:
# root> gulp build
# root> gulp build-min
# However there are needed dist directories in node_modules so do NOT ignore

# NOTE: ./docs can be re-built in by
# root> gulp docco

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# npm-debug
npm-debug.log*

# Vim swp-files
*.swp

# the following are all not expected to appear but included for completeness
# Compiled source 
*.com
*.class
*.dll
*.exe
*.o
*.so
 
# Packages 
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
